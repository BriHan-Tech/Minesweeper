/**
 * Interface for an object that contains data about the x and y position.
 *    x: number                 Col the cell is in
 *    y: number                 Row the cell is in
 */
export default interface iPos {
  x: number;
  y: number;
}
